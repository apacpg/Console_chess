using Jogo_Xadrez.Entities.Board;
using Jogo_Xadrez.Entities.Chess;
using Jogo_Xadrez.UI;
using System;

namespace Jogo_Xadrez
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            GameBoard board = new GameBoard(8, 8);

            board.PlacePiece(new Tower(Color.Black, board), new Position(0, 0));
            board.PlacePiece(new Horse(Color.Black, board), new Position(0, 1));
            board.PlacePiece(new Bishop(Color.Black, board), new Position(0, 2));
            board.PlacePiece(new Queen(Color.Black, board), new Position(0, 3));
            board.PlacePiece(new King(Color.Black, board), new Position(0,4));
            board.PlacePiece(new Bishop(Color.Black, board), new Position(0, 5));
            board.PlacePiece(new Horse(Color.Black, board), new Position(0, 6));
            board.PlacePiece(new Tower(Color.Black, board), new Position(0, 7));

            for (int j = 0; j < board.columns; j++)
                board.PlacePiece(new Pawn(Color.Black, board), new Position(1, j));

            board.PlacePiece(new Tower(Color.White, board), new Position(board.lines-1, 0));
            board.PlacePiece(new Horse(Color.White, board), new Position(board.lines - 1, 1));
            board.PlacePiece(new Bishop(Color.White, board), new Position(board.lines - 1, 2));
            board.PlacePiece(new Queen(Color.White, board), new Position(board.lines - 1, 3));
            board.PlacePiece(new King(Color.White, board), new Position(board.lines - 1, 4));
            board.PlacePiece(new Bishop(Color.White, board), new Position(board.lines - 1, 5));
            board.PlacePiece(new Horse(Color.White, board), new Position(board.lines - 1, 6));
            board.PlacePiece(new Tower(Color.White, board), new Position(board.lines - 1, 7));

            for (int j = 0; j < board.columns; j++)
                board.PlacePiece(new Pawn(Color.Black, board), new Position(board.lines - 2, j));

            Screen.DisplayBoard(board);
            */
        }
    }
}
